package com.ps27.railway.enums;

/** Kind of railway asset that can require maintenance. */
public enum AssetType {
    TRACK_SECTION,
    SIGNAL,
    TUNNEL,
    BRIDGE,
    STATION,
    LEVEL_CROSSING,
    OVERHEAD_EQUIPMENT,
    PLATFORM
}
